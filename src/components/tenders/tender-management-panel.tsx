"use client";

import Link from "next/link";
import { useState } from "react";

import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { FieldLabel, Input } from "@/components/ui/input";

type Tender = { id: string; reference_number: string; title: string; procuring_organization?: string | null; status: string; submission_deadline?: string | null; estimated_value?: number | null; currency_code?: string | null };

export function TenderManagementPanel({ tenders, canCreate }: { tenders: Tender[]; canCreate: boolean }) {
  const [open, setOpen] = useState(false); const [message, setMessage] = useState(""); const [saving, setSaving] = useState(false);
  async function create(form: HTMLFormElement) { setSaving(true); setMessage(""); const body = Object.fromEntries(new FormData(form).entries()); const response = await fetch("/api/tenders", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ title: body.title, referenceNumber: body.referenceNumber, procuringOrganization: body.procuringOrganization, tenderType: body.tenderType, estimatedValue: body.estimatedValue, submissionDeadline: body.submissionDeadline }) }); const data = await response.json().catch(() => ({})); setSaving(false); if (!response.ok) { setMessage(data.error || "Could not create tender."); return; } window.location.href = `/workspace/tenders/${data.tender.id}`; }
  return <>{canCreate ? <Card className="mt-8 p-5 sm:p-6"><div className="flex items-center justify-between gap-4"><div><h2 className="text-lg font-semibold text-[var(--betanor-navy)]">Tender register</h2><p className="mt-1 text-sm text-[var(--betanor-muted)]">Create a controlled tender workspace for proposals, guarantees, requirements, and final submission.</p></div><Button onClick={() => setOpen((value) => !value)}>{open ? "Close" : "New tender"}</Button></div>{open ? <form className="mt-5 grid gap-4 md:grid-cols-2" onSubmit={(event) => { event.preventDefault(); void create(event.currentTarget); }}><div><FieldLabel required htmlFor="tender-title">Tender title</FieldLabel><Input id="tender-title" name="title" required /></div><div><FieldLabel htmlFor="tender-reference">Reference number</FieldLabel><Input id="tender-reference" name="referenceNumber" placeholder="BTNR-TND-2026-00001" /></div><div><FieldLabel htmlFor="tender-org">Procuring organization</FieldLabel><Input id="tender-org" name="procuringOrganization" /></div><div><FieldLabel htmlFor="tender-type">Tender type</FieldLabel><Input id="tender-type" name="tenderType" placeholder="Open tender, RFQ, framework…" /></div><div><FieldLabel htmlFor="tender-value">Estimated value (ETB)</FieldLabel><Input id="tender-value" name="estimatedValue" type="number" min="0" step="0.01" /></div><div><FieldLabel htmlFor="tender-deadline">Submission deadline</FieldLabel><Input id="tender-deadline" name="submissionDeadline" type="datetime-local" /></div><div className="md:col-span-2 flex items-center gap-3"><Button type="submit" disabled={saving}>{saving ? "Creating…" : "Create tender"}</Button>{message ? <span className="text-sm text-[var(--betanor-danger)]">{message}</span> : null}</div></form> : null}</Card> : null}<Card className="mt-6 overflow-hidden"><div className="border-b border-[var(--betanor-border)] px-5 py-4"><h2 className="font-semibold text-[var(--betanor-navy)]">Tender pipeline</h2><p className="mt-1 text-xs text-[var(--betanor-muted)]">{tenders.length} visible tender{tenders.length === 1 ? "" : "s"} · server-scoped by your permissions.</p></div>{tenders.length ? <div className="divide-y divide-[var(--betanor-border)]">{tenders.map((tender) => <Link key={tender.id} href={`/workspace/tenders/${tender.id}`} className="flex flex-col justify-between gap-3 px-5 py-4 transition hover:bg-blue-50/30 sm:flex-row sm:items-center"><div><p className="font-semibold text-[var(--betanor-navy)]">{tender.title}</p><p className="mt-1 text-xs text-[var(--betanor-muted)]">{tender.reference_number} · {tender.procuring_organization || "Organization not recorded"}</p></div><div className="flex items-center gap-4"><div className="text-right text-xs text-[var(--betanor-muted)]"><p>{tender.submission_deadline ? new Date(tender.submission_deadline).toLocaleString("en-ET") : "Deadline not set"}</p><p className="mt-1">{tender.estimated_value ? `${tender.currency_code || "ETB"} ${Number(tender.estimated_value).toLocaleString()}` : "Value not set"}</p></div><Badge tone={tender.status === "SUBMITTED" || tender.status === "AWARDED" ? "success" : tender.status === "READY_FOR_SUBMISSION" ? "warning" : "info"}>{tender.status.replaceAll("_", " ")}</Badge></div></Link>)}</div> : <p className="px-5 py-12 text-center text-sm text-[var(--betanor-muted)]">No tenders are visible yet.</p>}</Card></>;
}
