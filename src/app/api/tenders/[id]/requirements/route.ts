import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { resolveWorkspace } from "@/lib/workspace-context";
export const dynamic = "force-dynamic";
async function auth() { const supabase = await createClient(); const access = await resolveWorkspace(supabase); return { supabase, access }; }
export async function POST(request: Request, { params }: { params: Promise<{ id: string }> }) { const { id } = await params; const { supabase, access } = await auth(); if (!access.workspaceId || !access.permissions.has("tender.edit")) return NextResponse.json({ error: "Tender edit permission is required." }, { status: 403 }); const body = await request.json().catch(() => ({})); const { data, error } = await supabase.from("tender_requirements").insert({ tender_id: id, title: String(body.title || "").trim(), is_mandatory: body.isMandatory !== false }).select("*").single(); if (error) return NextResponse.json({ error: error.message }, { status: 400 }); return NextResponse.json({ requirement: data }, { status: 201 }); }
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { const { id } = await params; const { supabase, access } = await auth(); if (!access.workspaceId || !access.permissions.has("tender.edit")) return NextResponse.json({ error: "Tender edit permission is required." }, { status: 403 }); const body = await request.json().catch(() => ({})); const { data, error } = await supabase.from("tender_requirements").update({ is_complete: Boolean(body.isComplete), completed_by: body.isComplete ? access.userId : null, completed_at: body.isComplete ? new Date().toISOString() : null }).eq("id", body.id).eq("tender_id", id).select("*").maybeSingle(); if (error) return NextResponse.json({ error: error.message }, { status: 400 }); if (!data) return NextResponse.json({ error: "Requirement not found." }, { status: 404 }); return NextResponse.json({ requirement: data }); }
