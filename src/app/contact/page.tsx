import Link from "next/link";
import { redirect } from "next/navigation";

import { PublicHeader } from "@/components/navigation/public-header";
import { SiteFooter } from "@/components/navigation/site-footer";
import { PageHero } from "@/components/public/page-hero";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { createClient } from "@/lib/supabase/server";

async function submitContact(data: FormData) {
  "use server";
  const supabase = await createClient();
  const { error } = await supabase.rpc("submit_public_contact_request", { name_input: String(data.get("name") ?? "").trim(), email_input: String(data.get("email") ?? "").trim(), phone_input: String(data.get("phone") ?? "").trim(), organization_input: String(data.get("organization") ?? "").trim(), topic_input: String(data.get("topic") ?? "").trim(), requirements_input: String(data.get("requirements") ?? "").trim() });
  redirect(error ? "/contact?error=1" : "/contact?sent=1");
}

export default async function ContactPage({ searchParams }: { searchParams: Promise<{ sent?: string; error?: string }> }) {
  const query = await searchParams;
  return <><PublicHeader /><main><PageHero eyebrow="Contact Betanor" title="Let’s start with the right conversation.">Whether you are assessing a project, comparing options, or seeking dependable long-term support, Betanor can help frame the next practical step.</PageHero><section className="mx-auto grid max-w-7xl gap-8 px-6 py-14 lg:grid-cols-[.72fr_1.28fr] lg:px-8"><div className="space-y-6"><Card><CardContent><h2 className="text-xl font-semibold text-[var(--betanor-navy)]">Contact Betanor</h2><p className="mt-3 leading-7 text-[var(--betanor-muted)]">Bole, Woreda 3, Eltek Building 103<br />Addis Ababa, Ethiopia</p><p className="mt-4 text-sm font-semibold leading-6 text-[var(--betanor-blue)]"><a href="tel:+251982010088">+251 98 201 0088</a><br /><a href="tel:+251917911604">+251 91 791 1604</a><br /><a href="mailto:info@betanor.et">info@betanor.et</a></p></CardContent></Card><Card><CardContent><h2 className="text-xl font-semibold text-[var(--betanor-navy)]">Customer portal</h2><p className="mt-3 leading-7 text-[var(--betanor-muted)]">Authenticated customers can keep every request, quotation, delivery update, invoice, and support conversation in one place.</p><Link href="/customer/login" className="mt-5 inline-flex text-sm font-semibold text-[var(--betanor-blue)]">Open customer sign in →</Link></CardContent></Card></div><Card><CardContent><h2 className="text-xl font-semibold text-[var(--betanor-navy)]">Send a message</h2><p className="mt-2 text-sm leading-6 text-[var(--betanor-muted)]">Use the form for a consultation, partnership question, support enquiry, or general business message. A Betanor team member will route it from the CRM.</p>{query.sent ? <div className="mt-6 rounded-xl border border-emerald-200 bg-emerald-50 p-5 text-sm leading-6 text-emerald-950">Thank you. Your message has been received by the Betanor team.</div> : <>{query.error ? <div className="mt-6 rounded-xl border border-red-200 bg-red-50 p-5 text-sm leading-6 text-red-800">We could not submit the message. Please check the fields and try again.</div> : null}<form action={submitContact} className="mt-7 grid gap-5 sm:grid-cols-2"><label className="text-sm font-semibold text-[var(--betanor-navy)]">Name<Input name="name" required minLength={2} className="mt-2" /></label><label className="text-sm font-semibold text-[var(--betanor-navy)]">Email<Input name="email" type="email" required className="mt-2" /></label><label className="text-sm font-semibold text-[var(--betanor-navy)]">Organization<Input name="organization" className="mt-2" /></label><label className="text-sm font-semibold text-[var(--betanor-navy)]">Phone<Input name="phone" type="tel" className="mt-2" /></label><label className="text-sm font-semibold text-[var(--betanor-navy)] sm:col-span-2">Topic<Input name="topic" required className="mt-2" /></label><label className="text-sm font-semibold text-[var(--betanor-navy)] sm:col-span-2">How can we help?<textarea name="requirements" required minLength={10} rows={6} className="mt-2 w-full rounded-lg border border-[var(--betanor-border)] px-3 py-2 text-sm" /></label><div className="sm:col-span-2"><Button type="submit" size="lg">Send message</Button></div></form></>}</CardContent></Card></section></main><SiteFooter /></>;
}
