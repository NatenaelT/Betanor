// The profile editor is shared by staff and customer identities. Keeping one
// server implementation prevents the two portals from drifting apart.
export { default } from "@/app/workspace/profile/page";
